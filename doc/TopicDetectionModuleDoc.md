# Topic 检测模块设计文档

## 文档状态

- 文档类型：模块设计文档
- 所属流程：Topic 检测阶段
- 当前阶段：MVP阶段
- MVP 范围：MVP阶段不进行Topic建模模块开发

## 功能描述

Topic 检测模块负责基于解析后的机器人数据推理关键 topic，识别运动 topic、状态 topic、动作 topic、末端负载对象 topic 和电机驱动 topic 等。

## 输入

待补充。补充时需要说明：

- 请求来源。
- 必需字段。
- 可选字段。
- 上游模块产物依赖。

## 输出

待补充。补充时需要说明：

- 输出数据结构。
- 输出文件或存储位置。
- 下游模块消费方式。

## 架构设计

待补充。建议包含：

- 核心类或服务。
- 模块边界。
- 与其他模块的依赖关系。
- 可扩展点。

## 接口定义

待补充。接口示例应使用合法 JSON 或 Python schema，并明确字段类型、默认值和单位。

## 功能逻辑

待补充。建议按执行顺序描述主流程、分支流程和失败流程。

## 数据 schema

待补充。schema 需要说明字段名、类型、单位、是否必需、默认值和示例。

## 异常处理规则

MVP 阶段默认采用 fail-fast 策略。

需要补充：

- 哪些错误直接失败。
- 哪些错误允许退化处理。
- 日志必须包含哪些定位字段。
- 是否需要输出空结果。

## MVP 验收标准

待补充。建议至少包含：

1. 使用 `tests/train_data_1.mcap` 或其上游产物可以跑通。
2. 正常输入能输出合法结构化结果。
3. 缺失必需字段时能给出明确错误。
4. 空结果场景有明确输出约定。

## 更新记录

- 2026-07-07：统一文档格式，补充待设计章节。
